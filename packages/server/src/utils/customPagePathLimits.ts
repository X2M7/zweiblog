/**
 * Shared limits for paths inside a multi-file custom-page project. Public
 * routing must not reject a project path that the upload API accepted.
 */
export const CUSTOM_PAGE_MAX_PATH_BYTES = 4096;
export const CUSTOM_PAGE_MAX_PATH_SEGMENTS = 64;
